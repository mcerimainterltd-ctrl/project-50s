name: xamepage_flutter
description: 100% Native XamePage Mobile App by Gibson Agbor.
version: 2.1.0+4
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  socket_io_client: ^2.0.3
  # Upgraded this to fix the Registrar error
  flutter_webrtc: ^0.12.0
  flutter_secure_storage: ^9.0.0

flutter:
  uses-material-design: true
