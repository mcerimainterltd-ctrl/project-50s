name: Build XamePage APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Recreate Android Project Harness
        run: |
          cd flutter_version
          # Remove any old, broken android files
          rm -rf android
          # Force recreate the android folder properly
          flutter create --platforms android .
          # Clean the build cache
          flutter clean
          flutter pub get

      - name: Build APK
        run: |
          cd flutter_version
          flutter build apk --release --no-tree-shake-icons
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: flutter_version/build/app/outputs/flutter-apk/app-release.apk
